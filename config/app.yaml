app_name: invest-notify
log_level: INFO

data:
  raw_file: data/raw/prices_raw.csv
  sqlite_file: data/raw/prices_raw.sqlite
  curated_file: data/curated/prices_curated.csv
  plot_dir: data/plots
  table_dir: data/tables

source:
  provider: twse   # mock | twse | live

window:
  low_days: 20
  high_days: 10
